
	<h1>Contacts</h1>
	<ul>
	<% Contact.each do |contact| %>
	<li><a href="/contacts/<%= contact.id %>"><%= contact.first_name %> <%= contact.last_name %> &lt<%= contact.email %>&gt (<%= contact.notes %>)</a> </li>
	<% end %>
